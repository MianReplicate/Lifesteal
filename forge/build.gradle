apply from: '../loader.gradle'

loom {
    launches {
        client {
            arg "--username", "DevGoose###"
        }

        /*data {
            arg "--existing", project(":common").file("src/main/resources/").absolutePath
        }*/
    }
}